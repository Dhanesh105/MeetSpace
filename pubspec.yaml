name: calendar_booking_app
description: A Calendar Booking App with Flutter frontend and Node.js backend
version: 1.0.0+1

environment:
  sdk: ">=2.19.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  http: ^1.1.0
  intl: ^0.17.0
  provider: ^6.0.5
  table_calendar: ^3.0.9
  flutter_form_builder: ^8.0.0
  form_builder_validators: ^8.6.1
  mongo_dart: ^0.9.3
  shared_preferences: ^2.2.2
  dropdown_button2: ^2.3.9

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.3

flutter:
  uses-material-design: true
